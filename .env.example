# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key

# Optional: Analytics
VITE_PLAUSIBLE_DOMAIN=millarx.com.au

# ============================================
# SUPABASE STORAGE SETUP
# ============================================
#
# Create a storage bucket called 'media' with these folders:
#
# media/
# ├── logos/
# │   ├── millarx-logo.svg        (main logo for header)
# │   ├── millarx-logo-white.svg  (white logo for footer/dark bg)
# │   ├── millarx-logo-dark.svg   (dark variant)
# │   └── millarx-icon.svg        (icon only)
# ├── images/
# │   ├── og-image.png            (1200x630 for social sharing)
# │   ├── hero-home.jpg
# │   ├── hero-employers.jpg
# │   └── cars/
# │       ├── tesla-model-3.jpg
# │       ├── tesla-model-y.jpg
# │       └── ... (other car images)
# └── videos/
#     └── intro.mp4
#
# Make sure to set the bucket to PUBLIC for media assets
# Storage > media bucket > Policies > Enable public access

# ============================================
# MXDRIVEIQ API INTEGRATION
# ============================================
# These are used by the Edge Function to forward leads to mxDriveIQ
#
# MXDRIVEIQ_API_URL=https://mxdriveiq.com.au
# MXDRIVEIQ_API_KEY=your_api_key_here
#
# Set these in Supabase Dashboard:
# Project Settings > Edge Functions > Secrets
#
# The Edge Function will:
# 1. Receive new leads from the database webhook
# 2. Transform data to mxDriveIQ format
# 3. POST to mxDriveIQ API for auto-quoting
# 4. Update lead status in Supabase

# ============================================
# EDGE FUNCTION SETUP
# ============================================
#
# Deploy the Edge Function:
# npx supabase functions deploy process-lead --project-ref YOUR_PROJECT_REF
#
# Set secrets:
# npx supabase secrets set MXDRIVEIQ_API_URL=https://mxdriveiq.com.au
# npx supabase secrets set MXDRIVEIQ_API_KEY=your_key
#
# Create Database Webhook (in Supabase Dashboard):
# - Table: quote_requests
# - Events: INSERT
# - URL: https://YOUR_PROJECT.supabase.co/functions/v1/process-lead
